services:
  postgres:
    image: postgres:16
    container_name: medisphere-db
    restart: always
    environment:
      POSTGRES_PASSWORD: yourpassword
      POSTGRES_DB: medisphere
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: